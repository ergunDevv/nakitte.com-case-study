// In normal we  should define the data models that represent the entities and concepts in our application.
// But because of our api sends us just info about games that's why i didn't add this part.
// We can define like down below : 

// export interface Product {
//     id?: string;
//     name: string;
//     price: number;
//     description?: string;
//     thumbnailUrl?: string;
//   }
  
//   export interface Cart {
//     product: Product;
//     quantity: number;
//   }